export const generatePagination = (currentPage: number, totalPages: number) => {
    // Check if its higher than limitation
    
    // Check if its higher than

    // Check if its less than

    // Otherwise return default value of an array of numbers
}